Files in `claude/` are symlinked to `~/.claude/` and files in `git/hooks/` are symlinked to `~/.git-hooks/`. When adding a new file that needs global installation, add the symlink command to the appropriate section in `README.md`.
